let json = require("./pokemon.json")

console.log(json)

/*
function nombrePokemon (name){
  for(let i = 0; i < pokemon.results.length; i++){
    if(name == pokemon.results[i].name){
      return `pokemon encontrado, su url es: ${pokemon.results[i].url}`;
    }
  }
  return 'no se encontro ningun pokemon con ese nombre.'
};

let resultado = nombrePokemon("own-tempo");

console.log(resultado);*/



/*
let resultado = pokemon.results.find(elemento => elemento.name === "own-tempo");

console.log(resultado);
*/